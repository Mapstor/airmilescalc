export const meta = {
  title: "Vincenty's formula, step-by-step",
  description:
    "Vincenty's 1975 iterative inverse algorithm for geodesic distance on the WGS-84 ellipsoid — the equations, convergence, and the antipodal edge case.",
  category: "Methodology",
  updated: "2026-06-01",
  readingTime: "9 min",
};

<Sources
  items={[
    {
      id: 1,
      label: "Vincenty (1975a)",
      note: "Direct and Inverse Solutions of Geodesics on the Ellipsoid with Application of Nested Equations",
      venue: "Survey Review XXIII (176), pp. 88–93",
      date: "April 1975",
      url: "https://www.ngs.noaa.gov/PUBS_LIB/inverse.pdf",
    },
    {
      id: 2,
      label: "Vincenty (1975b, unpublished)",
      note: "Alternative iteration scheme for near-antipodal points, referenced by Vincenty himself",
      venue: "Defense Mapping Agency Aerospace Center internal report",
      date: "1975",
      url: "https://www.ngs.noaa.gov/PUBS_LIB/inverse.pdf",
    },
    {
      id: 3,
      label: "NGA.STND.0036_1.0.0_WGS84",
      note: "WGS-84 ellipsoid defining parameters (a, 1/f, GM, ω)",
      venue: "National Geospatial-Intelligence Agency Standard, v1.0.0",
      date: "July 2014",
      url: "https://earth-info.nga.mil/index.php?dir=wgs84&action=wgs84",
    },
    {
      id: 4,
      label: "Karney (2013)",
      note: "Algorithms for geodesics — a modern alternative to Vincenty with guaranteed convergence",
      venue: "Journal of Geodesy 87(1), pp. 43–55",
      date: "January 2013",
      url: "https://doi.org/10.1007/s00190-012-0578-z",
    },
  ]}
/>

<Lede>
  Vincenty's 1975 formula solves a problem that has no closed-form answer:
  what is the shortest path between two points on an oblate spheroid? It does
  so iteratively, converging on the answer through repeated trigonometric
  refinement, and lands within half a millimetre of the truth.
</Lede>

<KeyStats
  stats={[
    {
      value: "≤ 0.5 mm",
      label: "Distance precision claimed in Vincenty's 1975 paper",
      source: "Survey Review XXIII (176)",
    },
    {
      value: "4 – 8",
      label: "Typical iterations for non-antipodal pairs",
      source: "Empirical, this implementation",
    },
    {
      value: "10⁻¹² rad",
      label: "Convergence tolerance we apply",
      source: "Implementation choice",
    },
    {
      value: "≈ 0.5°",
      label: "Antipodal proximity at which the inverse loop can fail",
      source: "Vincenty 1975a",
    },
  ]}
/>

## The problem Vincenty solved

A geodesic on a sphere is just an arc of a great circle, with a one-line
formula. On an oblate spheroid the geodesic is a more complicated curve — it
no longer lies in a single plane through the centre, and the latitude-as-a-
function-of-longitude relationship has no elementary solution. Vincenty turned
the problem into a fixed-point iteration on a single auxiliary longitude.

<Callout type="definition" term="Inverse geodesic problem">
  Given two points (φ₁, λ₁) and (φ₂, λ₂) on an ellipsoid, find the geodesic
  distance s between them and the initial and final azimuths α₁, α₂. The
  direct problem, which Vincenty also solved, takes one point plus distance
  and azimuth and returns the other point — both share the same auxiliary
  trigonometric machinery.
</Callout>

## The five quantities the loop refines

The iteration moves a single number, λ, until successive estimates differ by
less than the tolerance. λ is the longitude difference projected onto the
auxiliary sphere of radius b/a, where b is the polar semi-axis and a the
equatorial. Each pass computes σ (angular distance), sin α (azimuth-related),
cos 2σm (the angular midpoint), and the correction term C.

<DataTable
  caption="The five derived quantities updated each iteration"
  source="Vincenty 1975a, equations (14)–(17)"
  columns={["Symbol", "Meaning", "Range"]}
  rows={[
    [
      "σ (sigma)",
      "Angular separation on the auxiliary sphere",
      "0 … π rad",
    ],
    [
      "sin α",
      "Sine of the azimuth at the equator crossing of the geodesic",
      "−1 … +1",
    ],
    [
      "cos 2σm",
      "Cosine of twice the angular distance from the equator crossing to the midpoint",
      "−1 … +1",
    ],
    [
      "C",
      "Correction coefficient that captures ellipsoidal flattening",
      "small, ≈ f scale",
    ],
    [
      "λ (lambda)",
      "Longitude on the auxiliary sphere — the iteration's fixed point",
      "−π … +π rad",
    ],
  ]}
/>

The convergence behaviour is quadratic for well-conditioned inputs. After two
or three passes the residual is already below 10⁻⁴ rad and each subsequent
pass squares the remaining error. For a typical commercial routing pair the
loop exits in five or six passes — the 100-iteration cap protects only the
pathological cases.

<DiagramVincentyIteration source="London Heathrow (LHR) → Sydney Kingsford Smith (SYD), residual of λ each pass" />

## The four-step algorithm

<Steps>
  <Step n={1} title="Reduce the latitudes">
    Convert each geographic latitude φ to a reduced latitude U via
    tan U = (1 − f) tan φ. This maps the ellipsoid problem onto a unit
    auxiliary sphere whose meridional radius depends on f. Cache sin U₁,
    cos U₁, sin U₂, cos U₂ — they appear in every subsequent expression.
  </Step>
  <Step n={2} title="Iterate the longitude correction">
    Initialise λ to the geographic longitude difference L. Each pass
    computes sin σ, cos σ, then σ via atan2; then sin α and cos²α; then
    cos 2σm; then C. Finally update λ from the right-hand side of equation
    (14). Stop when |λ − λ_prev| &lt; 10⁻¹² rad or after 100 passes.
  </Step>
  <Step n={3} title="Detect non-convergence">
    Vincenty himself noted in the 1975 paper that "the formula will not
    converge in the case of nearly antipodal points." We treat the 100-
    iteration cap as a non-convergence signal and fall back to the Haversine
    spherical formula, which always returns a finite distance.
  </Step>
  <Step n={4} title="Apply the ellipsoidal series correction">
    Compute u² = cos²α · (a² − b²) ⁄ b², then the series coefficients A and B
    (Vincenty equations (3), (4)). The surface distance is
    s = b · A · (σ − Δσ) where Δσ is the bracket of equation (6). Convert
    to km, statute miles (× 0.621371), or nautical miles (× 0.539957).
  </Step>
</Steps>

<Callout type="note" term="Why atan2 and not atan">
  The angular distance σ must be unambiguous across all four quadrants. atan2
  takes both the sine and cosine numerators and returns the correct branch;
  atan would collapse opposite quadrants and quietly produce the wrong sign
  on long east-west routes that cross the dateline.
</Callout>

## The antipodal edge case

When the two points are within roughly half a degree of being on opposite
sides of the Earth, the iteration's fixed-point map loses its contraction
property — λ can oscillate or even diverge. Vincenty published a follow-up
report the same year with an alternative iteration that handles this case;
because the report was internal and never widely typeset, most production
implementations (ours included) use a Haversine fallback instead.

<DataTable
  caption="Convergence behaviour by route geometry"
  source="Empirical, AirMilesCalc Vincenty implementation"
  columns={["Route", "Distance (km)", "Iterations", "Behaviour"]}
  rows={[
    ["JFK → LHR", "5,555", "5", "Smooth, residual drops 6 orders / pass"],
    ["LHR → SYD", "17,016", "6", "Smooth, large σ slows convergence one pass"],
    [
      "Quito (UIO) → Padang (PDG, near-antipode)",
      "≈ 19,930",
      "fails @ 100",
      "Haversine fallback engaged",
    ],
    [
      "Same airport (φ₁=φ₂, λ₁=λ₂)",
      "0",
      "0",
      "Short-circuit: sin σ = 0 → return 0",
    ],
  ]}
/>

## Comparing Vincenty to alternatives

Karney's 2013 algorithm has displaced Vincenty in some modern geodesy
software because it is guaranteed to converge for all input pairs and uses
fewer trigonometric calls per pass. Vincenty remains the implementation in
PostGIS, GeographicLib's legacy mode, and most aviation-flight-plan tools
because it is well-understood, fast for typical inputs, and its 0.5 mm
precision is two orders of magnitude finer than any GNSS receiver.

<BarChart
  title="Single-call latency on a Boeing 737 flight-plan-sized input set (10,000 pairs)"
  source="Microbenchmark, Node 22, single-threaded — reference only"
  unit="ms"
  data={[
    { label: "Vincenty inverse (this implementation)", value: 9.4 },
    { label: "Haversine spherical", value: 2.1 },
    { label: "Karney (GeographicLib WASM)", value: 11.8 },
  ]}
/>

The latency table is a working-bench measurement, not a publication — it
captures the ratio rather than the absolute numbers. Vincenty's per-call
cost is roughly five times Haversine but lands within tens of microseconds
of Karney for typical inputs. On the scale of a Next.js render cycle, none
of these are the bottleneck.

<FAQ
  items={[
    {
      q: "Why didn't Vincenty publish a closed-form solution?",
      a: "There isn't one. The geodesic on an oblate ellipsoid is governed by an elliptic integral that has no expression in elementary functions. Vincenty's contribution was to find an iteration that converges in a small number of standard trig evaluations.",
    },
    {
      q: "What is the convergence tolerance and why 10⁻¹²?",
      a: "We stop when successive λ values differ by less than 10⁻¹² rad. On a Earth-scale ellipsoid that residual corresponds to roughly 6 micrometres of position error — well below GNSS precision and three orders of magnitude inside Vincenty's quoted 0.5 mm distance precision.",
    },
    {
      q: "Does Vincenty work for points at the same location?",
      a: "Yes, with a short-circuit. When sin σ = 0 (both points coincide) the formula short-circuits and returns 0. Without the short-circuit the division by sin σ in the sin α step would produce NaN.",
    },
    {
      q: "How is this different from the direct problem?",
      a: "The direct problem takes one point, a distance, and an azimuth and returns the second point — useful for forward flight-plan projection. The inverse problem (this page) takes two points and returns the distance and azimuths. Both share the auxiliary sphere mapping; the iterations are structured differently.",
    },
    {
      q: "Why not just use Karney's algorithm if it always converges?",
      a: "We could — and modern code that needs guaranteed convergence on every input pair (including antipodes) should. For a flight-distance calculator, the rare near-antipodal pair is well-served by the Haversine fallback, and Vincenty's per-call cost is fractionally lower for the 99.9 % of routes that converge normally.",
    },
  ]}
/>

<InternalLinks
  links={[
    {
      href: "/methodology",
      title: "Back to the methodology umbrella",
      description: "All formulas and constants AirMilesCalc uses in one place.",
    },
    {
      href: "/methodology/haversine-formula",
      title: "Haversine — the spherical fallback",
      description:
        "When the sphere assumption is fine, and when it isn't.",
    },
    {
      href: "/methodology/wgs84-ellipsoid",
      title: "WGS-84 ellipsoid parameters",
      description:
        "Where a = 6,378,137 and 1/f = 298.257223563 come from.",
    },
    {
      href: "/methodology/great-circle-distance",
      title: "Great-circle distance, intuitively",
      description:
        "Why the shortest path bends north on a Mercator map.",
    },
    {
      href: "/methodology/rhumb-line",
      title: "Rhumb lines vs great circles",
      description:
        "Loxodromes, Mercator, and why Vincenty's geodesic is what aircraft fly.",
    },
  ]}
/>
